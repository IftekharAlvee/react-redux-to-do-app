import React from 'react';

const ToDoList = ({item}) => {
    // console.log(item);
    return (
        
        <li class="list-group-item list-group-item-info ">{item}</li>
        
    );
};

export default ToDoList;